#!/usr/bin/env sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
  if command -v systemctl >/dev/null 2>&1; then
    systemctl stop aegis-daemon.service >/dev/null 2>&1 || true
    systemctl disable aegis-daemon.service >/dev/null 2>&1 || true
  fi
fi
